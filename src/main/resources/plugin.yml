name: "${project.artifactId}"
prefix: "${project.name}"
description: "${project.description}"
author: SirBlobman

main: "com.SirBlobman.enderpearl.cooldown.EnderpearlCooldown"
version: "${project.version}"

softdepend:
- MVdWPlaceholderAPI
- PlaceholderAPI